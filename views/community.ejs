<!DOCTYPE html>
<html>
<head>
    <% include ./partials/head%>
</head>
<body>

<header>
    <%include ./partials/header2%>
</header>

<main>

  <div class="container" style="margin-top:50px">
    <h2><%= commName%></h2>
    <p>The .table-striped class adds zebra-stripes to a table:</p>
    <table class="table table-striped">
      <thead class="text-center">
      <tr>
        <th>#</th>
        <th>제목</th>
        <th>작성자</th>
        <th>날짜</th>
        <th>조회수</th>
        <th>추천수</th>
      </tr>
      </thead>
      <tbody>
      <ul>
          <% for (var i = 0 ; i < records.length ; i ++) { %>
      <tr>
        <td> <a><%=i+1%></a></td>
        <td> <a href="/community/<%= commName%>/<%=records[i].id%>"><%=records[i].title%></a></td>
        <td> <a href="/community/<%= commName%>/<%=records[i].id%>/show_profile"><%=records[i].author%></a></td>
        <td> <a src="/community/<%= commName%>/<%=records[i].id%>"><%=records[i].date%></a></td>
        <td>조회수</td>
        <td>추천수</td>

            <%}%>
        </ul>
      </tr>
      </tbody>
    </table>


    <div class="clearfix">

      <button type="button" class="float-right btn btn-dark mr-2 mx-auto" style="width:200px;" onclick=window.location="/community/<%=commName%>/write">Write</button>
    </div>
    <div class="text-center">
      <ul class="pagination justify-content-center">
        <li class="page-item"><a class="page-link" href="#">Previous</a></li>
        <li class="page-item"><a class="page-link" href="#">1</a></li>
        <li class="page-item active"><a class="page-link" href="#">2</a></li>
        <li class="page-item"><a class="page-link" href="#">3</a></li>
        <li class="page-item"><a class="page-link" href="#">Next</a></li>
      </ul>

    </div>
  </div>

</main>

<footer class="fixed-bottom">
    <%include ./partials/footer%>
</footer>

</body>
</html>
